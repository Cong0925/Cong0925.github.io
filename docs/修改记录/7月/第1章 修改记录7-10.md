# 修改记录

## 2026-07-10

### 提问： `[14:15]`

1. 提交推送所有内容

### 提交推送 `[14:16]` - `[14:17]`

- 操作描述：将当前所有修改提交并推送到远程仓库
- 修改文件：home/css/style.css, home/index.html, home/js/main.js, CLAUDE.md, docs/修改记录/
- 提交信息：feat: 添加项目指令和修改记录规范，更新主页样式和功能
- 提交结果：成功推送到 origin/master

### 其他修改

- 修改内容...

### 新增项目卡片 `[17:46]`

- **操作描述**：新增3个项目到主页项目卡片
- **修改文件**：home/js/main.js, home/index.html
- **新增项目**：
  - `vue3-ts-model`：Vue3 + TypeScript 项目模板，集成 Element Plus、Pinia、富文本编辑器
  - `hugoBasicExample`：基于 Hugo Clarity 主题的个人博客系统，支持双语和登录发布
  - `student_club`：学生社团管理系统，含前台展示和后台管理
- **修改内容**：
  1. 在 `projects` 数组中添加3个新项目对象（id: 6, 7, 8）
  2. 更新 `index.html` 中项目数量显示（2 → 8）
- **项目描述生成**：通过阅读各项目本地代码，分析技术栈和核心功能后生成

### CLAUDE.md 新增指令 `[17:48]`

- **操作描述**：在 CLAUDE.md 中添加「新增项目目录初始化」操作指令
- **修改文件**：CLAUDE.md
- **新增指令内容**：
  - 触发条件：用户要求添加新项目时自动执行
  - 目录命名规范：kebab-case 格式
  - 标准目录结构：index.html + css/ + js/ + imgs/ + demo/（可选）
  - 初始文件内容模板
  - 执行时机：在主页项目数据添加后立即创建

### 新增练习项目状态 `[17:52]`

- **操作描述**：新增 `practice`（练习项目）状态类型，用于标记早期练手项目
- **修改文件**：home/js/main.js, home/index.html, home/css/style.css
- **修改内容**：
  1. 在 `main.js` 中添加 `PROJECT_STATUS` 状态配置对象
  2. 将3个新项目的 `status` 字段从 `completed` 改为 `practice`
  3. 在 `index.html` 筛选按钮中添加"练习项目"选项
  4. 在 `style.css` 中添加 `.status-practice` 样式（紫色主题）
- **状态说明**：
  - `practice`：练习项目，用于早期练手项目
  - 样式：紫色背景 + 紫色文字，与其他状态区分明显

### 新增项目类型 `[17:56]`

- **操作描述**：新增 `template`（模版类）和 `article`（文章管理）两种项目类型
- **修改文件**：home/js/main.js, home/index.html
- **修改内容**：
  1. 在 `PROJECT_TYPES` 中添加 `template` 和 `article` 类型配置
  2. 在 `index.html` 筛选按钮中添加"模版类"和"文章管理"选项
  3. 将 Vue3 TS Model 的 `type` 从 `library` 改为 `template`
- **类型说明**：
  - `template`：模版类，图标 `fas fa-clone`
  - `article`：文章管理，图标 `fas fa-newspaper`
- **项目类型分布**：
  - 模版类：Vue3 TS Model
  - 网页应用：SpendNote, Node.js代码生成器, AJ-Report, Hugo Blog, Student Club
  - 停工：jQuery SVG D3 Demo
  - 桌面软件：Write Helper

### 补充其他类型筛选 `[17:57]`

- **操作描述**：在筛选按钮中添加"其他"类型选项
- **修改文件**：home/index.html
- **修改内容**：在类型筛选组末尾添加 `other` 类型按钮
- **完整类型列表**：桌面软件、小程序、移动应用、网页应用、终端工具、开源库、模版类、文章管理、其他

### 新增项目目录结构 `[18:00]`

- **操作描述**：为4个新增项目创建对应的目录文件夹结构
- **创建目录**：
  - `vue3-ts-model/` - Vue3 TS Model 项目目录
  - `hugoBasicExample/` - Hugo Blog 项目目录
  - `student_club/` - Student Club 项目目录
  - `jquery-svg-d3-demo/` - jQuery SVG D3 Demo 项目目录
- **每个目录包含**：
  - `index.html` - 项目主页（含功能介绍和技术栈）
  - `css/style.css` - 主样式文件（各项目使用不同主题色）
  - `js/main.js` - 主脚本文件
  - `imgs/` - 图片资源目录（空）
  - `demo/` - 演示目录（空）
- **主题色分配**：
  - vue3-ts-model: 蓝色 (#409eff)
  - hugoBasicExample: 渐变紫 (#667eea)
  - student_club: 橙色 (#e6a23c)
  - jquery-svg-d3-demo: 灰色 (#909399)
- **同步更新**：修改 `home/js/main.js` 中各项目的 `homepage` 字段指向对应目录
